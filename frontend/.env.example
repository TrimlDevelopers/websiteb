# Production site URL (no trailing slash) — used for canonical URLs, sitemap, and Open Graph
VITE_SITE_URL=https://www.triboundtech.com

# Backend API URL in production (leave empty in dev to use Vite proxy)
# VITE_API_URL=https://api.triboundtech.com
